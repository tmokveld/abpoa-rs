[package]
name = "abpoa-ffi-sys"
version = "0.1.0"
edition = "2024"
links = "abpoa"
build = "build.rs"
license = "MIT"
description = "Raw FFI bindings to the abPOA partial order aligner."

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1"
bindgen = "0.72"
glob = "0.3"
